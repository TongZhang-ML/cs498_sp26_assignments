\uplevel{%
  \noindent\textbf{Part 2: Multiple Choice}\\
  Please clearly circle your answer.
  If you change your answer, please erase your previous marking cleanly.
  Each choice is graded separately and is worth 1 point.
}


\question[4]
Multi-head attention computes \(H\) attention outputs and then combines them. Which of the following statements is correct? \textbf{Select all that apply.}
\begin{enumerate}
\item Different heads can use different learned projection matrices, which can lead to different attention patterns.
\item Concatenating the head outputs typically increases the feature dimension before a final linear projection maps back to \(\dmodel\).
\item Using more heads makes causal masking unnecessary in a decoder.
\item Multi-head attention can be viewed as computing several weighted sums in parallel and then mixing them.
\end{enumerate}
\begin{soln}
A,B,D
\end{soln}



\question[4]
(\textit{PyTorch-level concept}) A common implementation pattern for attention is:
\[
\texttt{scores}\leftarrow \frac{QK^\top}{\sqrt{d_k}},\qquad
\texttt{scores}\leftarrow \texttt{scores}+\texttt{mask},\qquad
\texttt{attn}\leftarrow \texttt{softmax}(\texttt{scores}).
\]
Which of the following statements is correct? \textbf{Select all that apply.}
\begin{enumerate}
\item A causal mask is used in the encoder so that the encoder attends only to previous source tokens. 
\item Adding \texttt{mask} before softmax is a way to enforce that some \((i,j)\) pairs receive near-zero attention weight.
\item If \texttt{mask} uses large negative values for disallowed pairs, those pairs receive negligible probability after softmax.
\item The main purpose of the mask is to change the value vectors \(V\) by setting some entries of \(V\) to zero. 
\end{enumerate}
\begin{soln}
B, D
\end{soln}



\question[4]
Consider a decoder-only Transformer trained for next-token prediction on sequences \((y_1,\ldots,y_L)\). Which of the following statements is correct? \textbf{Select all that apply.}
\begin{enumerate}
\item During training, we can compute a loss for each position \(t\) using the predicted distribution for \(y_{t+1}\) and the known target token.
\item During inference, cross-attention to an encoder is required for any Transformer model.
\item During inference, tokens are generated sequentially because the next token is unknown until it is produced.
\item During training, causal masking is unnecessary because the targets are known. 
\end{enumerate}
\begin{soln}
A,C
\end{soln}

\question[4]
Consider a decoder--encoder model with decoder states \(S_\dec \in \rR^{L_\dec \times \dmodel}\) and encoder states \(H_\enc \in \rR^{L_\enc \times \dmodel}\). Which statements correctly describe cross-attention? \textbf{Select all that apply.}
\begin{enumerate}
\item Cross-attention typically needs a causal mask to prevent attending to future decoder positions. 
\item The attention weight matrix has size \(L_\dec \times L_\enc\) for each head.
\item For each head \(h\), \(Q^{(h)} = S_\dec W^{Q,h}\) and \(K^{(h)} = H_\enc W^{K,h}\).
\item In cross-attention, both \(K^{(h)}\) and \(V^{(h)}\) are computed from \(H_\enc\).
\end{enumerate}
\begin{soln}
B,C,D

\end{soln}


\question[4]
Which of the following statements about training versus inference in a decoder-only Transformer is correct? \textbf{Select all that apply.}
\begin{enumerate}
\item In training with teacher forcing, all positions can be computed in parallel within a layer because the target tokens are known.
\item In inference, generation is sequential because token \(y_{t+1}\) is unknown until the model produces it.
\item Even in training, causal masking is used so that position \(t\) does not use information from target positions \(>t\) when predicting \(y_{t+1}\).
\item In inference, causal masking is unnecessary because the model already knows future tokens.
\end{enumerate}
\begin{soln}
A,B,C

\end{soln}


\question[4]
Consider the following pseudocode for masked attention logits before \(\text{softmax}\):
\[
\text{scores} = QK^\top/\sqrt{d_k},\qquad
\text{scores}[\,\text{mask}=0\,] = -\infty,\qquad
A=\text{softmax}(\text{scores}).
\]
Which statement is correct?
\begin{enumerate}
\item This masking is primarily to make attention computations faster by reducing the asymptotic complexity from \(O(L^2)\) to \(O(L)\).
\item A padding mask is used to prevent attention to PAD tokens that were added only for batching.
\item Setting masked logits to a very negative value makes the corresponding \(\text{softmax}\) weights close to zero.
\item A causal mask is used to prevent a decoder position \(t\) from attending to positions \(>t\) during next-token prediction.
\end{enumerate}
\begin{soln}
C
\end{soln}

\question[4]
Which of the following statements about Rotary Positional Embedding (RoPE) are correct? \textbf{Select all that apply.}

\begin{enumerate}
\item RoPE is applied only to the value vectors. 
\item In RoPE, attention scores depend on relative positions through $(j-i)$.
\item RoPE changes the norm (magnitude) of query and key vectors.
\item RoPE rotates query and key vectors using position-dependent rotations. 
\end{enumerate}

\begin{soln}
B, D
\end{soln}


\question[4]
Which statements are correct about BERT-style MLM? \textbf{Select all that apply.}
\begin{enumerate}
\item MLM is identical to decoder-only next-token prediction.
\item Some input tokens are masked and predicted.
\item Both left and right context can be used.
\item MLM is a common pretraining objective for encoder-style models.
\end{enumerate}
\begin{soln}
B, C, D
\end{soln}

\question[4]
A fine-tuned model starts repeating phrases. Which first action is most reasonable? \textbf{Select all that apply.}
\begin{enumerate}
\item Keep learning rate fixed and train much longer.
\item Ignore validation behavior and continue training.
\item Lower the learning rate or stop earlier.
\item Assume repetition always implies underfitting.
\end{enumerate}
\begin{soln}
C
\end{soln}

\question[4]
Which statement correctly describes self-instruct style generation? \textbf{Select all that apply.}
\begin{enumerate}
\item It requires full manual authoring of every new instruction.
\item It is a one-shot process with no iteration.
\item It never uses seed tasks.
\item It commonly bootstraps from seed tasks and iteratively adds filtered generations.
\end{enumerate}
\begin{soln}
D
\end{soln}

\question[4]
Which statements are correct about dataset mixing in instruction tuning? \textbf{Select all that apply.}
\begin{enumerate}
\item Mixture composition can change held-out metrics.
\item Weighting strategies can matter, not just total data size.
\item Equal weights are always optimal.
\item Mixing has no effect once model size is fixed.
\end{enumerate}
\begin{soln}
A, B
\end{soln}

\question[4]
Which habits are appropriate when auditing instruction-tuned model behavior? \textbf{Select all that apply.}
\begin{enumerate}
\item Evaluate across held-out tasks or formulations.
\item Inspect failure cases linked to data quality issues.
\item Compare results across different data-mixture designs.
\item Use only training loss to claim broad generalization.
\end{enumerate}
\begin{soln}
A, B, C
\end{soln}

\question[4]
Which statements align with cautionary interpretation of emergence claims? \textbf{Select all that apply.}
\begin{enumerate}
\item Evaluation design has no effect on perceived thresholds. 
\item A discrete metric can make gradual improvement appear abrupt.
\item Capability-level emergence and smooth loss scaling can coexist.
\item Capability thresholds on specific tasks can appear even when training loss changes smoothly.
\end{enumerate}
\begin{soln}
B,C,D.
\end{soln}


% multiple choice
\question[4]
Which statements are consistent with scaling-law intuition? \textbf{Select all that apply.}
\begin{enumerate}
\item Test loss often follows a smooth power-law trend with increased scale in studied regimes.
\item Increasing parameters, data, and compute in a balanced way is usually more effective than scaling only one factor.
\item Scaling laws imply that architecture shape is always irrelevant.
\item Scaling results remove the need for empirical evaluation on downstream tasks.
\end{enumerate}
\begin{soln}
A,B.
\end{soln}


\question[4]
You need a single concise answer generated from trusted material
only. Which setups are most appropriate?
 \textbf{Select all that apply.}
\begin{enumerate}
\item Provide the context text and ask the model to summarize the  relevant information without additional constraints.
\item Ask for long reasoning traces without final-answer constraint.
\item We only need to ask the model to answer concisely based on the provided material.
\item Provide delimited trusted context and require a fixed output format.
\end{enumerate}
\begin{soln}
D.
\end{soln}


\question[4]
Under a fixed compute budget, which strategies are most aligned with compute-optimal scaling observations?
\textbf{Select all that apply.}
\begin{enumerate}
\item Maximize model size while keeping training tokens very small.
\item Keep model and data near a balanced tradeoff curve rather than overemphasizing one side.
\item Fully converge the smallest model possible, regardless of data scale.
\item Use identical token-per-parameter ratios for all model families and settings.
\end{enumerate}
\begin{soln}
B.
\end{soln}

\question[4]
Which statements about precision choices are accurate?  \textbf{Select all that apply.}
\begin{enumerate}
\item BF16/FP16 are always numerically safer than FP32 for any model and any setup.
\item FP32 is faster than BF16/FP16 on recent tensor-core GPUs.
\item In mixed precision training, BF16/FP16 are the preferred training precision for optimizer states.
\item Precision choices affect both speed and memory footprint.
\end{enumerate}
\begin{soln}
D.
\end{soln}

\question[4]
Which statements about ZeRO-Offload are correct? \textbf{Select all that apply.}
\begin{enumerate}
\item It can lower peak GPU memory by moving optimizer/master states to CPU.
\item It incurs little communication overhead.
\item It may reduce training throughput if CPU-GPU bandwidth is the bottleneck.
\item It eliminates the need for mixed precision.
\end{enumerate}
\begin{soln}
A,C.
\end{soln}

\question[4]
Which statements about LoRA design are correct? \textbf{Select all that apply.}
\begin{enumerate}
\item LoRA updates all base-model weight entries during fine-tuning.
\item Rank $r$ and scaling $\alpha$ are key hyperparameters.
\item LoRA typically uses less optimizer-state memory than full-parameter fine-tuning.
\item Applying LoRA to attention projections is a common practical choice.
\end{enumerate}
\begin{soln}
B,C,D.
\end{soln}

% Local Variables:
% TeX-master: "../exam.tex"
% End:
